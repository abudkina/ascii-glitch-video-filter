{
  "name": "ascii-glitch-video-filter",
  "version": "1.0.0",
  "description": "ASCII / Glitch фильтр видео с веб-камеры в реальном времени",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test",
    "lint": "tsc --noEmit",
    "lighthouse": "npm run build && npx --yes lighthouse http://127.0.0.1:4273 --preset=desktop --only-categories=performance,accessibility,best-practices --view"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18"
  }
}
